<footer>
	<div class="gradient"></div>

	<div class="footer-main">
		<div class="container">
			<div class="brand">
				<img
					src="/logo.png"
					alt="Logo do PagoMeia"
				/>

				<div class="brand-copy">
					<h3>PagoMeia</h3>

					<p>
						A sua meia-entrada para a cultura pop.
					</p>
				</div>
			</div>

			<div class="links">
				<div class="link-group">
					<h4>Navegação</h4>

					<a href="/">Início</a>
					<a href="/blog">Blog</a>
					<a href="/#jogos">Jogos</a>
				</div>

				<div class="link-group">
					<h4>Redes</h4>

					<a
						href="https://youtube.com/@pagomeia"
						target="_blank"
						rel="noopener noreferrer"
					>
						YouTube
					</a>

					<a href="#">Instagram</a>
					<a href="#">TikTok</a>
				</div>
			</div>
		</div>
	</div>

	<div class="bottom">
		<div class="bottom-container">
			<span>© 2026 PagoMeia</span>
			<span>Todos os direitos reservados.</span>
		</div>
	</div>
</footer>

<style>
	footer {
		margin-top: 90px;
		background:
			radial-gradient(
				circle at top left,
				rgba(124, 58, 237, 0.22),
				transparent 36%
			),
			#120d18;
		color: #ffffff;
	}

	.gradient {
		height: 4px;
		background: linear-gradient(
			90deg,
			#7c3aed 0%,
			#a855f7 36%,
			#ec4899 70%,
			#ef4444 100%
		);
	}

	.footer-main {
		padding: 46px 0 38px;
	}

	.container {
		width: min(1500px, calc(100% - 56px));
		margin: 0 auto;
		display: flex;
		align-items: center;
		justify-content: space-between;
		gap: 70px;
	}

	.brand {
		display: flex;
		align-items: center;
		gap: 18px;
	}

	.brand img {
		width: 66px;
		height: 66px;
		flex-shrink: 0;
		border: 2px solid rgba(255, 255, 255, 0.16);
		border-radius: 50%;
		object-fit: cover;
		box-shadow: 0 14px 34px rgba(124, 58, 237, 0.2);
	}

	.brand-copy h3 {
		margin: 0;
		color: #ffffff;
		font-size: 27px;
		line-height: 1;
		letter-spacing: -0.04em;
	}

	.brand-copy p {
		margin: 10px 0 0;
		color: #c9c1cf;
		font-size: 14px;
	}

	.links {
		display: flex;
		gap: 72px;
	}

	.link-group {
		min-width: 110px;
	}

	.link-group h4 {
		margin: 0 0 15px;
		color: #ffffff;
		font-size: 13px;
		text-transform: uppercase;
		letter-spacing: 0.09em;
	}

	.link-group a {
		display: block;
		margin-bottom: 9px;
		color: #c7becd;
		text-decoration: none;
		font-size: 13px;
		transition:
			color 0.2s ease,
			transform 0.2s ease;
	}

	.link-group a:hover {
		color: #c084fc;
		transform: translateX(3px);
	}

	.bottom {
		border-top: 1px solid rgba(255, 255, 255, 0.08);
	}

	.bottom-container {
		width: min(1500px, calc(100% - 56px));
		margin: 0 auto;
		padding: 18px 0;
		display: flex;
		justify-content: space-between;
		gap: 24px;
		color: #847b8c;
		font-size: 12px;
	}

	@media (max-width: 900px) {
		.container {
			width: min(100% - 36px, 760px);
			align-items: flex-start;
			flex-direction: column;
			gap: 38px;
		}

		.links {
			gap: 54px;
		}

		.bottom-container {
			width: min(100% - 36px, 760px);
		}
	}

	@media (max-width: 600px) {
		.footer-main {
			padding: 38px 0 32px;
		}

		.brand img {
			width: 58px;
			height: 58px;
		}

		.links {
			flex-direction: column;
			gap: 24px;
		}

		.bottom-container {
			flex-direction: column;
			gap: 5px;
			padding: 16px 0;
		}
	}
</style>